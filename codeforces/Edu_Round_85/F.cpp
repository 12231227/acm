#include <bits/stdc++.h>
using namespace std;
int main(){
	int n;
	scanf("%d",&n);
	for (int i = 1;i <= n; i++) scanf("%d",&a[i]);
	for (int i = 1;i <= n; i++) scanf("%d",&p[i]);
	int m;
	scanf("%d",&m);
	for (int i = 1;i <= m; i++) scanf("%d",&b[i]);
	f[0][0] = 0;
	for (int i = 1;i <= m; i++)
} 
